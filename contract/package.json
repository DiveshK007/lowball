{
  "name": "@lowball/contract",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compact": "compact compile src/lowball.compact src/managed/lowball",
    "compact:hello": "compact compile src/hello-world.compact src/managed/hello-world",
    "test": "vitest run",
    "test:compile": "npm run compact && vitest run"
  }
}
